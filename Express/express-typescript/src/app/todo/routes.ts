import { Router } from "express";

const router = Router();
//#region //*================= Routes ================

router.get('/')
router.get('/:id')

router.post('/')
router.post('/:id')

router.put('/:id')
router.delete('/:id')

//#endregion //*================= Routes ================


export default router;
